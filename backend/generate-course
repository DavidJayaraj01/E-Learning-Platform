#!/bin/bash
# Course Generation CLI Wrapper
# Makes it easy to run the CLI from anywhere

# Get the script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python CLI
python3 "$SCRIPT_DIR/course_cli.py" "$@"